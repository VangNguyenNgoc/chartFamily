const path = require("path");
const base64_encode = require("../utils/convertBase64");

function getDataCouple(listCouple, listData) {
  //   const listIdCouple = listCouple;
  if (listCouple.length > 0) {
    console.log("123");
    // let idNodesFamily = [];
    //display image of couple
    // let imageCouple = listCouple[listCouple.length - 1].id.idPerson.image;
    // const pathImageCouple = path.join(__dirname, "../public/img/", imageCouple);
    // listData.idNode.idCouple[listCouple.length - 1].id.idPerson.image =
    //   base64_encode(pathImageCouple);
  }
}

module.exports = getDataCouple;
